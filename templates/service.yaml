apiVersion: v1
kind: Service
metadata:
  name: {{ include "java-session.fullname" . }}
  labels:
    {{- include "java-session.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "java-session.selectorLabels" . | nindent 4 }}
